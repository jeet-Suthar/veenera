{
  "compilerOptions": {
    "baseUrl": ".",          // Set the base directory (root of the project)
    "paths": {
      "@/*": ["src/*"]       // Map @/ to the src/ folder
    }
  },
  "exclude": ["node_modules"] // Exclude node_modules from processing
}